{
  "name": "wind-forecast-app",
  "version": "1.0.0",
  "scripts": {
    "start": "node backend/server.js",
    "dev": "npm run dev --prefix frontend & npm run dev --prefix backend",
    "build": "npm run build --prefix frontend"
  },
  "dependencies": {
    "axios": "^1.6.7",
    "cors": "^2.8.5",
    "dotenv": "^16.4.1",
    "express": "^4.18.2"
  }
}
